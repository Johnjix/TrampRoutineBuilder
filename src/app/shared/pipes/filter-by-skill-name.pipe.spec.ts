import { FilterBySkillNamePipe } from './filter-by-skill-name.pipe';

describe('FilterBySkillNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySkillNamePipe();
    expect(pipe).toBeTruthy();
  });
});
