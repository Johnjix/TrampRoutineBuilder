import { FilterBySkillShapePipe } from './filter-by-skill-shape.pipe';

describe('FilterBySkillShapePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterBySkillShapePipe();
    expect(pipe).toBeTruthy();
  });
});
