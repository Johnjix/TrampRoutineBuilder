import { FilterByMaxDifficultyPipe } from './filter-by-max-difficulty.pipe';

describe('FilterByMaxDifficultyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByMaxDifficultyPipe();
    expect(pipe).toBeTruthy();
  });
});
